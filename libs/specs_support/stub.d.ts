export default class Stub extends React.PureComponent<any, any, any> {
    static propTypes: {
        children: PropTypes.Validator<object>;
    };
    constructor(props: any);
    constructor(props: any, context: any);
    render(): JSX.Element;
}
import React from "react";
import PropTypes from "prop-types";
